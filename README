This is a pretty simple implementation of Conway's game of life. The only twist is it outputs the video to a x264 mp4 file at a constant framerate.

There is a toy ffmpeg wrapper here that is minimally usable.
